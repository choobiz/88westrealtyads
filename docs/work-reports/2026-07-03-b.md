# 88 West Go-Landing — Work Report: 2026-07-03 (Part B)

**Theme:** Roll the winning inline-form hero out to all foreclosure LP variants; retire Variant A
**Total Hours:** 1.5

---

## Task 1: Inline-form hero on all variants + retire Variant A
**Project:** 88 West Go-Landing — Foreclosure (court-ordered) LP A/B/C/D test
**Date:** 2026-07-03
**Hours:** 1.5
**Category:** Enhancement

**Summary:**
Tracker + observation showed all conversions landing on Variant B, whose hero embeds the lead form inline above the fold. Made that inline-form hero the hero for **every** variant so no cohort is stuck with the low-converting control hero, and retired Variant A (which would have become a duplicate of B). The A/B/C/D test now isolates *inventory layout* under a single proven hero.

**Key Changes:**
- `proxy.ts` — `VARIANT_SPLIT` → `{B:0.50, C:0.75, D:1.0}` (A's 25% folds into B → B 50 / C 25 / D 25); `Variant` type `B|C|D`; stale `_lp_ab_cohort=A` cookies migrate to B and re-persist.
- `app/foreclosure-deals-vancouver/page.tsx` — always render `ForeclosureHeroVariantB`; removed the A branch, the lower `ForeclosureFormSection`, and now-unused `ForeclosureHero`/`ForeclosureFormSection` imports; default variant `B`.
- `scripts/track-server.mjs` — `SPLIT` mirror `{A:0, B:50, C:25, D:25}`; variant descriptions updated (A retained for historical dashboard data).

**Commits:** `4b5ee8f`

**Verification:** build clean; live on go.88westrealty.com — `?ab=B/C/D` each serve the inline-form hero (headline + `<form>` name/email/phone + "Get Today's Deal List") with their distinct body (B cards / C Portfolio Console / D explorer); Vercel cache MISS confirmed fresh. Baseline at change: 26 sessions, 3 leads — all 3 on B.

---

## Day Summary (Part B)

| # | Task | Hours | Category |
|---|------|-------|----------|
| 1 | Inline-form hero on all variants + retire A | 1.5 | Enhancement |
| | **Total** | **1.5** | |

## Commits

| Commit | Message |
|--------|---------|
| `4b5ee8f` | feat(foreclosure LP): inline-form hero on all variants; retire Variant A |

## Files Modified

| File | Tasks |
|------|-------|
| `proxy.ts` | 1 |
| `app/foreclosure-deals-vancouver/page.tsx` | 1 |
| `scripts/track-server.mjs` | 1 |

## Infrastructure / Deploy
- LP: `npx vercel --prod` → aliased go.88westrealty.com.
- Tracker: VM `git pull` + `pm2 restart 88w-track` (`/home/amir/88west-go-landing`, 127.0.0.1:8088).

## Follow-up
- Monitoring the variant dashboard over the next several days as the 50/25/25 split accumulates a comparable sample (current n is tiny). Attribution preserved via `experiment_variant` in the lead payload.
